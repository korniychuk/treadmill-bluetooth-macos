# 048 — Live smoke: reliability 035–047

> **Статус: done (partial — core path green)**  
> **Дата smoke:** 2026-07-11  
> **Когда:** после `scripts/install-daemon.sh` с кодом 035–047  
> **Цель:** подтвердить, что wiring-фиксы держатся на железе, не только в unit-тестах.

## Результат сессии 2026-07-11

| Сценарий | Статус | Заметки |
|---|---|---|
| Install + `tm doctor`/`status` | ✅ | LaunchAgent, AC scanning |
| S1 Connect + walk + widget | ✅ | presence Walking, 0x2ACD age 0s, workout in progress |
| S1 HR worn (Polar H10) | ✅ | ~83 bpm, battery 100%, contact live; widget ♥ |
| Default speed on walk start | ✅ | log: `control_source=default_speed` 0.5→2.0 (039 tag) |
| S2 Power-off | ⏭ not run this session | |
| S3 Strap off / frozen bpm | ⏭ not run | |
| S4–S6 Zone Hold paths | ⏭ zone_hold.enabled=false | |
| S8 `tm doctor` mid-walk | ✅ | full matrix healthy, no WARN |
| S9 recompute while live | ✅ earlier | refuses with clear error (044) |

**Оператор:** «вроде как работает» — core path (connect, walk, HR, widget, doctor) green.

### Incident mid-smoke (documented for ops)

1. After install, idle scan was healthy (`no FTMS within 15s`).
2. ~02:38 UTC: treadmill found → connect → **btleplug panic** on background thread:  
   `Got descriptors for a characteristic we don't know about`  
   (`btleplug-0.12` `corebluetooth/internal.rs`).
3. Process **did not exit** → KeepAlive no restart → every subsequent cycle  
   `err=start filtered BLE scan` (wedged CBCentralManager).
4. Recovery:  
   `launchctl kickstart -k gui/$(id -u)/com.korniychuk.treadmill-bluetooth-macos.daemon`  
   → scan healthy again → after console wake, connect + HR succeeded.

Follow-up work: [backlog/009](../backlog/009-btleplug-panic-wedges-ble-scan.md) — panic on discover must kill process or re-create adapter.

---

## Предусловия

- [x] `cargo build --release` + codesign + LaunchAgent
- [x] `tm` → `target/release/treadmill-bluetooth-macos`
- [x] daemon **running**, power `ac_scanning`
- [x] Дорожка **вкл** и в зоне BLE; H10 надет

### Полезные окна

```bash
tm doctor
tm status
tail -f ~/Library/Logs/treadmill-bluetooth-macos/daemon.log
rg -N 'control_source|HR |zone hold|no telemetry|start filtered' \
  ~/Library/Logs/treadmill-bluetooth-macos/daemon.log
```

### Recovery if scan dies (`start filtered BLE scan` spam)

```bash
launchctl kickstart -k "gui/$(id -u)/com.korniychuk.treadmill-bluetooth-macos.daemon"
# then wake treadmill console if needed (Yesoul may stop FTMS advertising when idle)
```

Do **not** run parallel `tm scan`/`tm connect`/Yesoul app while daemon owns the link.

### Zone Hold for S4–S6 (optional later)

```bash
tm zone on
tm zone list
tm zone target work
```

---

## Сценарии (чеклист на будущее)

### S1 — Connect + walk ✅ 2026-07-11

### S2 — Power-off treadmill while walking (**031 / 035 class**)

### S3 — Remove H10 / freeze contact (**033 / 035**)

### S4 — `tm zone off` mid-ramp (**032**)

### S5 — CLI speed vs Zone Hold (**039**)

### S6 — Safety floor no-op beeps (**041**)

### S7 — Engage / MORE_DATA sanity (**036**)

### S8 — Doctor only (**038**) ✅ mid-walk

### S9 — Recompute guard (**044**) ✅ refused while live

### S10 — Config atomic write (**037**)

---

## Связанное

- research 003 Phase 0.7, 004 Phase 0  
- tasks 035–047 (all **done** in code)  
- backlog 009 — btleplug panic wedges scan  
- logs: `~/Library/Logs/treadmill-bluetooth-macos/daemon.log`  
- db: `~/Library/Application Support/treadmill-bluetooth-macos/treadmill.db`
